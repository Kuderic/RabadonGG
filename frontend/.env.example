# Optional: base URL for the backend API.
# - Leave unset for production builds to use same-origin (relative) requests.
# - Set to http://localhost:8000 (the dev default) or another host if the API
#   is served from a different origin.
# VITE_API_BASE_URL=https://api.rabadon.gg
